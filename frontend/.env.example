# Sample mode is the deployment-safe default. Set false for authenticated, live backend data.
DASHBOARD_DEMO_MODE=true
BACKEND_URL=http://127.0.0.1:8000
APP_URL=http://localhost:3000
# Supabase Auth only; application data remains in the existing PostgreSQL database.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
